import SwiftUI

struct AboutView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("cool")
            }
            .navigationTitle("About")
        }
    }
}
